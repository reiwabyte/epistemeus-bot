import * as bail from 'baileys'
import chalk from 'chalk'

global.owner = {
    no: ['6283891882373'],
    name: 'Owner'
}

global.set = {
    prefix: ['.'],
    self: false
}

global.pair = {
    no: '6283891882373',
    isPair: true,
    sesi: 'session'
}

global.bail = bail
global.chalk = chalk
global.start = Date.now()
